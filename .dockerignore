# ── Dependencies (re-installed inside the container) ─────────
node_modules

# ── Build output (re-built inside the container) ─────────────
.next

# ── Git ──────────────────────────────────────────────────────
.git
.gitignore

# ── Docker files (not needed inside the image) ───────────────
Dockerfile
docker-compose.yml

# ── Docs ─────────────────────────────────────────────────────
README.md

# ── Env files (secrets are passed via docker-compose, not COPY) 
.env
.env.local
.env.example
.env.production